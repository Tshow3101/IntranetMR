@model IEnumerable<IntranetMundoRepresentaciones.Models.tb_traslado>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutReporte.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-inverse">
            <div class="panel-heading">
                <div class="panel-heading-btn">
                    <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default" data-click="panel-expand"><i class="fa fa-expand"></i></a>
                    <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-success" data-click="panel-reload"><i class="fa fa-repeat"></i></a>
                    <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-warning" data-click="panel-collapse"><i class="fa fa-minus"></i></a>
                    <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-danger" data-click="panel-remove"><i class="fa fa-times"></i></a>
                </div>
                <h4 class="panel-title" style="color:#d1158e;"><strong style="color:#d1158e;">@Html.ActionLink("Agregar Tarifa Aerea", "Create")</strong></h4>
            </div>
            <div class="panel-body">
                <table id="data-table" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>OPERADOR</th>
                            <th>DESTINO</th>
                            <th>FECHA VIGENCIA</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody style="text-transform:uppercase">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(model => item.id_traslado)
                                </td>
                                <td>
                                    @Html.DisplayFor(model => item.tb_usuario.nombreusuario)
                                </td>
                                <td>
                                    @Html.DisplayFor(model => item.tb_ciudad.nombreCiudad)
                                </td>
                                <td style="text-transform: uppercase">
                                    @String.Format("{0:yyyy-MM-dd}", item.fecha_vigencia)
                                </td>
                               
                                <td>
                                    <div class="divbtnopc">
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-default dropdown-toggle btnopc"
                                                    data-toggle="dropdown">
                                                <span> ACTION</span>
                                            </button>

                                            <ul class="dropdown-menu" role="menu">
                                                <li><a href="/Traslado/Edit/@item.id_traslado">EDITAR</a></li>
                                                <li><a href="/Traslado/Details/@item.id_traslado">DETALLE</a></li>
                                                <li><a href="/Traslado/Generatepdf/@item.id_traslado">DESCARGAR</a></li>
                                                <li><a onclick="dardebaja(@item.id_traslado)">ELIMINAR</a></li>
                                            </ul>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    // Permite eliminar archivo previamente adjunto
    function dardebaja(id) {
        var data = { id: id }
        $.ajax({
            type: "POST",
            url: "/Traslado/DardeBaja",
            data: JSON.stringify(data),
            contentType: "application/json",
            success: function (data) {
                if (data.status) {
                    sweetAlert({
                        title: 'Correcto',
                        text: 'El registro fue eliminado',
                        type: 'success'
                    }).then(function () {
                        window.location.reload();
                    });
                }
                else {
                    sweetAlert({
                        title: 'Error',
                        text: 'No se pudo eliminar registro',
                        type: 'error'
                    }).then(function () {
                        window.location.reload();
                    });

                }
            },
            error: function (ex) {
                console.log(ex)
            }
        })
    }
</script>
